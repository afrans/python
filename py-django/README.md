# Django com Python 3.14 em Docker

Este projeto é um exemplo de aplicação Django rodando em um container Docker usando Python 3.14.

---

## Create Project
docker compose run --rm web django-admin startproject core .

## Run Project
docker compose up

## Test Project
http://localhost:8000

## 🧠 O que é Django?

**Django** é um framework web **full-stack** escrito em Python.

### Principais características:

- Segue o padrão **MVT** (Model-View-Template).
- Inclui componentes como:
  - ORM (Mapeamento Objeto-Relacional)
  - Painel administrativo
  - Sistema de autenticação
  - Entre outros recursos prontos
- Ideal para aplicações **robustas**, com estrutura bem definida e administração integrada.
